name: ci

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest

    # Node 24: actions/checkout@v6 y actions/setup-python@v6 usan
    # Node 24 nativamente (sin env var). Solución definitiva post-S54.
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Compile check
        run: python -m compileall liquidator

      - name: Lint (ruff)
        run: ruff check liquidator/

      - name: Type check (mypy)
        run: mypy liquidator/ --ignore-missing-imports || true

      - name: Tests (pytest)
        run: pytest liquidator/tests -q --tb=short

      - name: KB freshness
        run: python scripts/check_kb_freshness.py
